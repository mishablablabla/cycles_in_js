const arr = [3, 5, 8, 16, 20, 23, 50];
    const resullt = [];

    // Пишем решение вот тут
    
    for (let i = 0; i < arr.length; i++) {
        resullt[i] = arr[i];
    }
    console.log(resullt)